---
name: biz-optimize
description: "Get optimization recommendations based on data"
argument-hint: "[area]"
---
Read and execute the command file at `commands/optimize.md` in the project root.

Arguments: $ARGUMENTS

Follow the full process defined in that command file:
1. Read `commands/optimize.md` for the command spec
2. Read `agents/05-insights-agent.md` for the agent system prompt
3. Read `skills/optimization-advisor/SKILL.md` for the skill instructions
4. Read relevant files in `skills/optimization-advisor/references/`
5. Execute the command and deliver the output
