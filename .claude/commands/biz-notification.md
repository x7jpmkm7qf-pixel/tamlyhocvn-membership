---
name: biz-notification
description: "Set up Telegram order notifications"
argument-hint: "[step]"
---
Read and execute the command file at `commands/notification.md` in the project root.

Arguments: $ARGUMENTS

Follow the full process defined in that command file:
1. Read `commands/notification.md` for the command spec
2. Read `agents/04-deliver-agent.md` for the agent system prompt
3. Read `skills/notification-setup-guide/SKILL.md` for the skill instructions
4. Read relevant files in `skills/notification-setup-guide/references/`
5. Execute the command and deliver the output
