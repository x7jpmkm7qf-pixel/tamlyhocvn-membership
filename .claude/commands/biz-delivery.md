---
name: biz-delivery
description: "Set up automatic product delivery"
argument-hint: "[method]"
---
Read and execute the command file at `commands/delivery.md` in the project root.

Arguments: $ARGUMENTS

Follow the full process defined in that command file:
1. Read `commands/delivery.md` for the command spec
2. Read `agents/04-deliver-agent.md` for the agent system prompt
3. Read `skills/delivery-setup-guide/SKILL.md` for the skill instructions
4. Read relevant files in `skills/delivery-setup-guide/references/`
5. Execute the command and deliver the output
