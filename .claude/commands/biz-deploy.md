---
name: biz-deploy
description: "Deploy landing page or website to Vercel"
argument-hint: "[method]"
---
Read and execute the command file at `commands/deploy.md` in the project root.

Arguments: $ARGUMENTS

Follow the full process defined in that command file:
1. Read `commands/deploy.md` for the command spec
2. Read `agents/04-deliver-agent.md` for the agent system prompt
3. Read `skills/vercel-deployment/SKILL.md` for the skill instructions
4. Read relevant files in `skills/vercel-deployment/references/`
5. Execute the command and deliver the output
