---
name: biz-landing-page
description: "Build a deployable landing page with Tailwind CSS"
argument-hint: "[product-type] [style]"
---
Read and execute the command file at `commands/landing-page.md` in the project root.

Arguments: $ARGUMENTS

Follow the full process defined in that command file:
1. Read `commands/landing-page.md` for the command spec
2. Read `agents/04-deliver-agent.md` for the agent system prompt
3. Read `skills/landing-page-builder/SKILL.md` for the skill instructions
4. Read relevant files in `skills/landing-page-builder/references/`
5. Use templates from `skills/landing-page-builder/templates/` if available
6. Execute the command and deliver the output
