---
name: biz-payment-embed
description: "Add SePay VietQR payment to landing pages"
argument-hint: "[pattern]"
---
Read and execute the command file at `commands/payment-embed.md` in the project root.

Arguments: $ARGUMENTS

Follow the full process defined in that command file:
1. Read `commands/payment-embed.md` for the command spec
2. Read `agents/04-deliver-agent.md` for the agent system prompt
3. Read `skills/payment-embed/SKILL.md` for the skill instructions
4. Read relevant files in `skills/payment-embed/references/`
5. Use templates from `skills/payment-embed/templates/` if available
6. Execute the command and deliver the output
